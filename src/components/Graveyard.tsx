import {useEffect,useState} from 'react';
import {ArrowLeft,ArrowRight,ArrowUpRight,Download,Crosshair,Loader2} from 'lucide-react';
import {api,date,duration,number} from '../lib/api';
import type {Placement} from '../lib/types';
import {Creative} from './Creative';
import {ShareControls} from './ShareControls';
export function Graveyard({notify,id}:{notify:(s:string)=>void;id?:string}){const [items,setItems]=useState<Placement[]|null>(null),[error,setError]=useState(''),[page,setPage]=useState(0),[more,setMore]=useState(false);useEffect(()=>{setError('');(id?api<Placement>(`/api/placements/${id}`).then(p=>({items:[p],hasMore:false})):api<{items:Placement[];hasMore:boolean}>(`/api/archive?page=${page}`)).then(d=>{setItems(d.items);setMore(d.hasMore);if(id&&d.items[0])document.title=`${d.items[0].name} | The Last Ad Graveyard`;}).catch(e=>setError(e.message));},[page,id]);return <main className="page graveyard"><div className="page-title"><span className="eyebrow">THEY OWNED THE PAGE. THEN THEY DIDN’T.</span><h1>{id?'THE FINAL RECORD.':'THE GRAVEYARD.'}</h1><p>A finite moment. A permanent place in internet history.</p></div>{id&&<a className="text-button back-graves" href="/archive"><ArrowLeft size={16}/> All dead ads</a>}{error&&<div className="error" role="alert">{error}</div>}{!items&&!error?<div className="loading"><Loader2 className="spin"/> Opening the records…</div>:items?.length?<div className={`grave-grid ${id?'detail':''}`}>{items.map(p=><article className="grave-record" key={p.id}><div className="grave-top"><span>{p.house?'HOUSE AD':p.complimentary?'COMPLIMENTARY':'SPONSORED'}</span><span>CASE / {p.id.slice(0,6).toUpperCase()}</span></div><a href={`/archive/${p.id}`} className="grave-creative"><Creative p={p}/><span className="verdict">{p.reason==='audience'?'KILLED.':p.status==='ended'?'ENDED.':p.status.toUpperCase()}</span></a><div className="grave-body"><h2>{p.name}</h2><p className="cause">{p.reason==='audience'?'Killed by the internet.':p.reason==='timeout'?'Reached the 24-hour limit.':p.reason==='operator'?'Ended by the operator.':'Still part of the experiment.'}</p><dl className="grave-metrics"><div><dt>TIME ALIVE</dt><dd>{duration(p)}</dd></div><div><dt>AUDIENCE HITS</dt><dd>{number(p.allowance-p.remaining)}</dd></div><div><dt>RECORDED VIEWS</dt><dd>{number(p.views)}</dd></div><div><dt>PRODUCT VISITS</dt><dd>{number(p.clicks)}</dd></div></dl><p className="grave-date">{p.ended_at?`${date(p.ended_at)} · ${new Date(Number(p.ended_at)).toLocaleTimeString('en-US',{hour:'2-digit',minute:'2-digit',timeZoneName:'short'})}`:'Waiting for the final curtain.'}{p.views==null?' · Detailed metrics kept private by the sponsor.':''}</p><ShareControls p={p} notify={notify} compact/><div className="grave-links">{p.status==='ended'&&<a href={`/share/${p.id}.svg`} download><Download size={16}/> Death certificate</a>}<a href={`/go/${p.id}`} target="_blank" rel="noopener noreferrer">Visit product <ArrowUpRight size={16}/></a></div></div></article>)}</div>:!error?<div className="empty-state"><Crosshair size={40}/><h2>NO VICTIMS. YET.</h2><p>The current ad is still breathing.</p><a className="button dark" href="/">Take your hit <ArrowRight size={18}/></a></div>:null}{!id&&(page>0||more)&&<div className="pagination"><button className="button" disabled={page===0} onClick={()=>setPage(page-1)}><ArrowLeft size={16}/> Newer</button><span>Page {page+1}</span><button className="button" disabled={!more} onClick={()=>setPage(page+1)}>Older <ArrowRight size={16}/></button></div>}</main>;}
