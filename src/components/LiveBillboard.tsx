import {useEffect,useRef,useState} from 'react';
import {Crosshair,ArrowUpRight,Check,Radio,Clock3,ArrowRight,Loader2} from 'lucide-react';
import {useLiveState} from '../hooks/useLiveState';
import {api,instrument,number,duration} from '../lib/api';
import type {Placement,Receipt} from '../lib/types';
import {Creative} from './Creative';
import {ShareControls} from './ShareControls';
import {AuctionPanel} from './AuctionPanel';
export function pressure(lives:number){if(lives<=5)return {key:'final',label:'FINAL LIVES',copy:'Every hit could be the last.'};if(lives<=25)return {key:'deathwatch',label:'DEATHWATCH',copy:'Bring a witness. It’s almost over.'};if(lives<=100)return {key:'danger',label:'DANGER ZONE',copy:'The end is getting closer.'};if(lives<=200)return {key:'pressure',label:'UNDER PRESSURE',copy:'The internet is making itself heard.'};return {key:'normal',label:'ALIVE. FOR NOW.',copy:'One ad owns the page. You decide when it leaves.'};}
export function LiveBillboard({notify}:{notify:(s:string)=>void}){
 const {data,error,refresh}=useLiveState();const [shown,setShown]=useState<Placement|null>(null),[death,setDeath]=useState<Placement|null>(null),[busy,setBusy]=useState(false),[receipt,setReceipt]=useState<Receipt|null>(null),[hit,setHit]=useState(false),[actionError,setActionError]=useState('');
 const lastId=useRef(''),latest=useRef(data),transition=useRef<ReturnType<typeof setTimeout>|null>(null),hitTimer=useRef<ReturnType<typeof setTimeout>|null>(null),sending=useRef(false);latest.current=data;
 function enter(){if(transition.current)clearTimeout(transition.current);transition.current=null;setDeath(null);if(latest.current)setShown(latest.current.current);}
 useEffect(()=>{if(!data)return;const p=data.current;if(lastId.current!==p.id){const old=lastId.current;lastId.current=p.id;if(old){setDeath(data.previous?.id===old?data.previous:null);if(transition.current)clearTimeout(transition.current);transition.current=setTimeout(enter,2400);}else setShown(p);void api(`/api/placements/${p.id}/view`,'POST',{}).catch(()=>{});}else if(!transition.current)setShown(p);document.title=`${p.remaining} lives left — ${p.name} | The Last Ad`;},[data]);
 useEffect(()=>{instrument('homepage_view');return()=>{if(transition.current)clearTimeout(transition.current);if(hitTimer.current)clearTimeout(hitTimer.current);};},[]);
 async function takeLife(){if(!data||sending.current)return;sending.current=true;setBusy(true);setActionError('');try{const result=await api(`/api/placements/${data.current.id}/end`,'POST',{});if(result.counted){setReceipt(result.receipt);setHit(true);hitTimer.current=setTimeout(()=>setHit(false),550);notify(`Hit #${result.receipt.ordinal} confirmed. ${result.receipt.remaining} lives remain.`);}else notify(result.stale?'That ad has already left the stage.':'Your one hit already counted.');await refresh();}catch(e){setActionError((e as Error).message);}finally{sending.current=false;setBusy(false);}}
 if(!data||!shown)return <main className="page"><div className="loading"><Loader2 className="spin"/> Connecting to the billboard…</div>{error&&<p role="alert">{error} <button className="button small" onClick={refresh}>Retry</button></p>}</main>;
 const p=shown,state=pressure(p.remaining),acted=data.acted||receipt?.placementId===p.id;const label=p.house?'HOUSE AD':p.complimentary?'COMPLIMENTARY':'SPONSORED';
 return <main className={`event-home state-${state.key} ${hit?'hit-confirmed':''}`}><div className="broadcast-line"><span><span className="live-dot"/> LIVE NOW</span><span className="broadcast-message">ONE AD. THE INTERNET HAS THE LAST WORD.</span><span>ROUND / {p.id.slice(0,6).toUpperCase()}</span></div><section className="live-arena" aria-label="Live ad and life counter"><div className="ad-stage"><div className="target-line"><h1>{p.name}</h1><span>{label}</span></div><Creative p={p}/><div className="ad-exit"><span>See it before it’s gone.</span><a href={`/go/${p.id}`} target={p.house?'_self':'_blank'} rel="noopener noreferrer">{p.house?'Put your ad here':'Visit '+p.name}<ArrowUpRight size={18}/></a></div></div><div className="life-console"><div className="danger-label"><Crosshair size={17}/>{state.label}</div><div className="life-count" key={`${p.id}:${p.remaining}`}>{p.remaining.toLocaleString('en-US')}</div><div className="lives-label">LIVES LEFT</div><div className="depletion" role="progressbar" aria-label="Lives remaining" aria-valuenow={p.remaining} aria-valuemin={0} aria-valuemax={p.allowance}><span style={{width:`${p.remaining/p.allowance*100}%`}}/></div><div className="depletion-label"><span>{number(p.allowance-p.remaining)} HITS TAKEN</span><span>{p.allowance} TOTAL LIVES</span></div><button className={`take-life ${acted?'spent':''}`} onClick={takeLife} disabled={acted||busy||Boolean(transition.current)}>{busy?<Loader2 className="spin" size={22}/>:acted?<Check size={22}/>:<Crosshair size={22}/>}<span>{acted?'YOUR HIT COUNTED':'TAKE ONE LIFE'}</span><strong>{acted?'✓':'−1'}</strong></button><p className="one-hit">You get one hit. At zero, the next ad takes over.</p><div className="hit-receipt" aria-live="polite">{receipt?.placementId===p.id?<><span>HIT #{receipt.ordinal} / VERIFIED</span><strong>{receipt.from} <ArrowRight size={15}/> {receipt.remaining}</strong></>:<><span>{state.copy}</span><Clock3 size={15}/></>}</div><div className="summon"><p>{acted?'WANT IT GONE FASTER?':'DON’T LET IT DIE ALONE.'}<br/><strong>Bring someone else.</strong></p><ShareControls p={p} receipt={receipt} notify={notify}/></div></div>{death&&<div className="death-overlay" role="status"><span className="eyebrow">{death.reason==='audience'?'THE INTERNET FINISHED IT.':death.reason==='timeout'?'TIME RAN OUT.':'THE OPERATOR ENDED IT.'}</span><h2>{death.name}<br/><em>{death.reason==='audience'?'IS DEAD.':'HAS LEFT.'}</em></h2><div className="death-stats"><span>{death.allowance-death.remaining} hits</span><span>{duration(death)} alive</span>{death.views!=null&&<span>{number(death.views)} views</span>}{death.clicks!=null&&<span>{number(death.clicks)} visits</span>}</div><button onClick={enter}>NEXT AD ENTERING <ArrowRight size={20}/></button></div>}</section>{(error||actionError)&&<div className="error" role="alert">{actionError||error} <button className="text-button" onClick={refresh}>Reconnect</button></div>}<section className="crowd-strip" aria-label="Real audience activity"><div><Radio size={17}/><strong>{data.hitsLast5Minutes}</strong><span>HITS IN THE LAST 5 MIN</span></div><div className="activity-ticker">{data.activity.length?data.activity.slice(0,3).map(e=><span key={e.id}>{e.kind==='hit'?`HIT #${e.ordinal} · ${e.remaining} LEFT`:e.kind==='refilled'?'+250 LIVES / REFILLED':'A NEW AD TOOK THE STAGE'}</span>):<span>Quiet right now. Be part of the next moment.</span>}</div><span className="real-label">REAL ACTIVITY ONLY</span></section><AuctionPanel data={data}/><section className="normal-queue"><div><span className="eyebrow">THE NORMAL QUEUE</span><h2>{data.queueCount} ADS WAITING.</h2><p>Keep your place in line. Auctions never take two turns in a row.</p></div><ol>{data.queue.length?data.queue.map((p,i)=><li key={p.id}><span>{String(i+1).padStart(2,'0')}</span><strong>{p.name}</strong><small>{p.complimentary?'Complimentary':'Sponsored'} · 500 lives</small></li>):<li className="no-queue">The line is open. Your ad could be next.</li>}</ol><a className="button dark" href="/sponsor" onClick={()=>instrument('sponsor_cta_clicked',p.id)}>Enter the experiment <ArrowUpRight size={17}/></a></section><section className="public-record"><div className="record-heading"><span className="eyebrow">THE RECORD. NO MADE-UP NUMBERS.</span><a href="/archive">The graveyard <ArrowUpRight size={18}/></a></div><div className="record-grid"><div><strong>{number(data.stats.killed_ads)}</strong><span>ADS KILLED BY THE CROWD</span></div><div><strong>{number(data.stats.total_hits)}</strong><span>AUDIENCE HITS</span></div><div><strong>{number(data.stats.shared_views)}</strong><span>PUBLICLY SHARED VIEWS</span></div><div><strong>{number(data.stats.shared_clicks)}</strong><span>PUBLICLY SHARED VISITS</span></div><div><strong>{number(data.stats.refills)}</strong><span>REFILLS COMPLETED</span></div></div><p>Sponsored and complimentary placements. House ads excluded. Views and visits include only advertisers who opted to share them.</p></section></main>;
}
